package easynotes.concerns;

/**
 * MVC controller for notes. Takes care of mapping between model and UI.
 * Its responsibility is to present data from model to UI and vice versa, to 
 * apply changes to data done in UI to model.
 * @author Milan
 */
public @interface NotesController {
    
}
